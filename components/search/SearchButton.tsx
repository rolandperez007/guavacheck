"use client";

export default function SearchButton() {
  return (
    <button
      style={{
        padding: "10px 20px",
        borderRadius: 10,
        cursor: "pointer",
      }}
    >
      Search
    </button>
  );
}